@using BarnamenevisanCompany.Application.Extensions
@using BarnamenevisanCompany.Domain.Enums.Common
@using BarnamenevisanCompany.Domain.Enums.DynamicPage
@using BarnamenevisanCompany.Domain.Shared
@using BarnamenevisanCompany.Infra.Data.Statics
@using BarnamenevisanCompany.Web.Helpers.TagHelpers
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model BarnamenevisanCompany.Domain.ViewModels.Admin.DynamicPage.AdminFilterDynamicPageViewModel

@{
    ViewData["Title"] = "لیست صفحات داینامیک";
}

@section Breadcrumb
{
    <admin-breadcrumb page-title="@ViewData["Title"]">
        <admin-breadcrumb-item last-active-item="true">@ViewData["Title"]</admin-breadcrumb-item>
    </admin-breadcrumb>
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="mb-4">
                    <h2 class="card-title">فیلتــر</h2>
                </div>
                <div>

                    <form asp-area="Admin"
                          asp-controller="DynamicPage"
                          asp-action="List"
                          method="get"
                          class="floating-labels"
                          id="filter-search"
                          no-validation>
                        <input asp-for="@Model.Page" type="hidden"/>
                        <div class="row filter-form justify-content-center">

                            <div class="col-12 col-md-3 mb-4">
                                <div class="form-group">
                                    <input asp-for="Title" class="form-control">
                                    <span class="bar"></span>
                                    <label asp-for="Title"></label>
                                </div>
                            </div>


                            <div class="col-12 col-md-3 mb-4 mt-3 mt-md-0">
                                <div class="form-group">
                                    <select class="form-control form-select" asp-for="ActiveStatus"
                                            asp-items="@Html.GetEnumSelectList<DynamicPageActiveStatus>()"></select>
                                    <span class="bar"></span>
                                    <label asp-for="ActiveStatus"></label>
                                </div>
                            </div>


                            <div class="col-12 col-md-3 mb-4 mt-3 mt-md-0">
                                <div class="form-group">
                                    <select class="form-control form-select" asp-for="DeleteStatus"
                                            asp-items="@Html.GetEnumSelectList<DeleteStatus>()"></select>
                                    <span class="bar"></span>
                                    <label asp-for="DeleteStatus"></label>
                                </div>
                            </div>


                            <div class="col-12 col-md-3 mb-4 mt-3 mt-md-0">
                                <div class="form-group">
                                    <select class="form-control form-select" asp-for="FilterOrderBy"
                                            asp-items="@Html.GetEnumSelectList<FilterOrderBy>()"></select>
                                    <span class="bar"></span>
                                    <label asp-for="FilterOrderBy"></label>
                                </div>
                            </div>

                            <div class="col-12 mt-3 d-md-flex">
                                    <div class="col-12 col-md-2 p-0 me-1">
                                        <button class="btn btn-rounded btn-outline-primary w-100" type="submit">جستجو
                                        </button>
                                    </div>
                                    @if (Model.HasFilters())
                                    {
                                        <div class="col-12 col-md-2 p-0 mt-3 mt-md-0">
                                            <a class="btn btn-rounded btn-danger w-100 text-nowrap"
                                               asp-area="Admin"
                                               asp-controller="DynamicPage"
                                               asp-action="List">حذف فیلتر ها
                                            </a>
                                        </div>
                                    }
                            </div>

                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
    <div class="col-12">
        <div class="card">
            <div class="card-body">

                <div class="mb-3 d-flex justify-content-between align-items-center">
                    <div>
                        <h2 class="card-title">@ViewData["Title"]</h2>
                    </div>
                    <div invoke-permission="@PermissionsName.CreateDynamicPage">
                        <a class="btn btn-rounded btn-success"
                           asp-area="Admin"
                           asp-controller="DynamicPage"
                           asp-action="Create">افزودن</a>
                    </div>
                </div>

                @if (Model.Entities is not null && Model.Entities.Any())
                {
                    <div class="table-responsive mb-4 border rounded-1">

                        <table class="table text-nowrap mb-0 align-middle">

                            <thead class="text-dark fs-4">
                            <tr>
                                <th>
                                    <h6 class="fs-4 fw-semibold mb-0">عنوان</h6>
                                </th>
                                <th>
                                    <h6 class="fs-4 fw-semibold mb-0">وضعیت صفحه</h6>
                                </th>
                                <th>
                                    <h6 class="fs-4 fw-semibold mb-0">تاریخ ایجاد</h6>
                                </th>
                                <th>
                                    <h6 class="fs-4 fw-semibold mb-0">عملیات</h6>
                                </th>
                            </tr>
                            </thead>

                            <tbody>
                            @foreach (var item in Model.Entities)
                            {
                                <tr>
                                    <td>
                                        <p class="mb-0 fw-normal fs-4 custom-text-truncate">@item.Title</p>
                                    </td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success-subtle text-success">فعال</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger-subtle text-danger">غیرفعال</span>
                                        }
                                    </td>
                                    <td>
                                        <p class="mb-0 fw-normal fs-4">@item.CreatedDate.ToShamsiDateTime()</p>
                                    </td>
                                    <td>
                                        <a  invoke-permission="@PermissionsName.UpdateDynamicPage"
                                            asp-area="Admin"
                                           asp-controller="DynamicPage"
                                           asp-action="Update"
                                           asp-route-id="@item.Id"
                                           asp-route-page="@Model.Page"
                                           class="text-warning removeAfterDelete @(item.IsDeleted ? "d-none" : "")"
                                           title="ویرایش">
                                            <iconify-icon icon="uil:edit" width="22" height="22"></iconify-icon>
                                        </a>
                                        @if (item.IsDeleted)
                                        {
                                            <a invoke-permission="@PermissionsName.DeleteOrRecoverDynamicPage" class="text-info" onclick="ConfirmRecover(this, '@Url.Action("DeleteOrRecover", "DynamicPage", new { area = "Admin", id = item.Id })')"
                                               title="بازگردانی">
                                                <iconify-icon icon="solar:undo-left-round-outline" width="22" height="22"></iconify-icon>
                                            </a>
                                        }
                                        else
                                        {
                                            <a invoke-permission="@PermissionsName.DeleteOrRecoverDynamicPage" class="text-danger" onclick="ConfirmDelete(this, '@Url.Action("DeleteOrRecover", "DynamicPage", new { area = "Admin", id = item.Id })')" title="حذف">
                                                <iconify-icon icon="solar:trash-bin-trash-outline" width="22" height="22"></iconify-icon>
                                            </a>
                                        }
                                    </td>
                                </tr>
                            }
                            </tbody>

                        </table>
                    </div>
                    <partial name="~/Areas/Admin/Views/Shared/_PagingPartial.cshtml" model="@Model.GetCurrentPaging()"/>
                }
                else
                {
                    <partial name="_NotFoundPartial"/>
                }
            </div>
        </div>
    </div>
</div>